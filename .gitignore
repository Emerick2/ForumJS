# Base de données SQLite (fichiers locaux, jamais sur git)
*.db
*.db-shm
*.db-wal

# Binaire compilé
forum
/forum
server
/server

# Variables d'environnement (mots de passe, clés secrètes)
.env

# Dossiers système
.DS_Store
Thumbs.db

go.mod
go.sum